{
  "name": "cv-website",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "publish": "npm run build && npx surge dist --domain andrea.kaist.id"
  },
  "dependencies": {
    "astro": "^5.17.1",
    "yaml": "^2.8.2"
  },
  "devDependencies": {
    "@types/node": "^25.5.0"
  }
}
